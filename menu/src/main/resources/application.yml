server:
  port: 8082
spring:
  application:
    name: menu

  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      database: menu
      password: rootpass
      port: 30001
      host: localhost
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
